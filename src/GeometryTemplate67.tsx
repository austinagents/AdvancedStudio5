import React from "react";

import {
  VoxelOctreeBody,
} from "./advanced-studio5/geometry-templates/product-templates-2/VoxelOctreeBody";

export type GeometryTemplate67Props = {
  imageSrc: string;
};

export const GeometryTemplate67:
  React.FC<
    GeometryTemplate67Props
  > = (
    props,
  ) => {
    return (
      <VoxelOctreeBody
        {...props}
      />
    );
  };
