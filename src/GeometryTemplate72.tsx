import React from "react";

import {
  ImplicitMetaballBody,
} from "./advanced-studio5/geometry-templates/product-templates-2/ImplicitMetaballBody";

export type GeometryTemplate72Props = {
  imageSrc: string;
};

export const GeometryTemplate72:
  React.FC<
    GeometryTemplate72Props
  > = (
    props,
  ) => {
    return (
      <ImplicitMetaballBody
        {...props}
      />
    );
  };
