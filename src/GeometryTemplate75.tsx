import React from "react";

import {
  ChainmailRingField,
} from "./advanced-studio5/geometry-templates/product-templates-2/ChainmailRingField";

export type GeometryTemplate75Props = {
  imageSrc: string;
};

export const GeometryTemplate75:
  React.FC<
    GeometryTemplate75Props
  > = (
    props,
  ) => {
    return (
      <ChainmailRingField
        {...props}
      />
    );
  };
