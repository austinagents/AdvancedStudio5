import React from "react";

import {
  DeformationLatticeBody,
} from "./advanced-studio5/geometry-templates/product-templates-2/DeformationLatticeBody";

export type GeometryTemplate76Props = {
  imageSrc: string;
};

export const GeometryTemplate76:
  React.FC<
    GeometryTemplate76Props
  > = (
    props,
  ) => {
    return (
      <DeformationLatticeBody
        {...props}
      />
    );
  };
