import React from "react";

import {
  AccordionPleatSurface,
} from "./advanced-studio5/geometry-templates/product-templates-2/AccordionPleatSurface";

export type GeometryTemplate70Props = {
  imageSrc: string;
};

export const GeometryTemplate70:
  React.FC<
    GeometryTemplate70Props
  > = (
    props,
  ) => {
    return (
      <AccordionPleatSurface
        {...props}
      />
    );
  };
