import React from "react";

import {
  HexagonalVolumeField,
} from "./advanced-studio5/geometry-templates/product-templates-2/HexagonalVolumeField";

export type GeometryTemplate59Props = {
  imageSrc: string;
};

export const GeometryTemplate59:
  React.FC<
    GeometryTemplate59Props
  > = (
    props,
  ) => {
    return (
      <HexagonalVolumeField
        {...props}
      />
    );
  };
