import React from "react";

import {
  RotationalLatheBody,
} from "./advanced-studio5/geometry-templates/product-templates-2/RotationalLatheBody";

export type GeometryTemplate74Props = {
  imageSrc: string;
};

export const GeometryTemplate74:
  React.FC<
    GeometryTemplate74Props
  > = (
    props,
  ) => {
    return (
      <RotationalLatheBody
        {...props}
      />
    );
  };
