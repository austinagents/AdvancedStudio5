import React from "react";

import {
  LamellaFanField,
} from "./advanced-studio5/geometry-templates/product-templates-2/LamellaFanField";

export type GeometryTemplate73Props = {
  imageSrc: string;
};

export const GeometryTemplate73:
  React.FC<
    GeometryTemplate73Props
  > = (
    props,
  ) => {
    return (
      <LamellaFanField
        {...props}
      />
    );
  };
