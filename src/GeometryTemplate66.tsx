import React from "react";

import {
  BooleanSliceVolume,
} from "./advanced-studio5/geometry-templates/product-templates-2/BooleanSliceVolume";

export type GeometryTemplate66Props = {
  imageSrc: string;
};

export const GeometryTemplate66:
  React.FC<
    GeometryTemplate66Props
  > = (
    props,
  ) => {
    return (
      <BooleanSliceVolume
        {...props}
      />
    );
  };
