import React from "react";

import {
  TensegrityProductCage,
} from "./advanced-studio5/geometry-templates/product-templates-2/TensegrityProductCage";

export type GeometryTemplate71Props = {
  imageSrc: string;
};

export const GeometryTemplate71:
  React.FC<
    GeometryTemplate71Props
  > = (
    props,
  ) => {
    return (
      <TensegrityProductCage
        {...props}
      />
    );
  };
