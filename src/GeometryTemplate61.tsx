import React from "react";

import {
  GeodesicFiberBody,
} from "./advanced-studio5/geometry-templates/product-templates-2/GeodesicFiberBody";

export type GeometryTemplate61Props = {
  imageSrc: string;
};

export const GeometryTemplate61:
  React.FC<
    GeometryTemplate61Props
  > = (
    props,
  ) => {
    return (
      <GeodesicFiberBody
        {...props}
      />
    );
  };
