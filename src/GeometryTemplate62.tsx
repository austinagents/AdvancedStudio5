import React from "react";

import {
  InsetPyramidField,
} from "./advanced-studio5/geometry-templates/product-templates-2/InsetPyramidField";

export type GeometryTemplate62Props = {
  imageSrc: string;
};

export const GeometryTemplate62:
  React.FC<
    GeometryTemplate62Props
  > = (
    props,
  ) => {
    return (
      <InsetPyramidField
        {...props}
      />
    );
  };
